name: Pipeline
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Copy files
        uses: actions/checkout@v3

      - name: Install pyython
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
